#pragma once

#include <Windows.h>

#include <vector>

namespace stuff {
  namespace types {
    using Offsets = std::vector<intptr_t>;
    using Pointer = intptr_t;
  }  // namespace types
}  // namespace stuff
